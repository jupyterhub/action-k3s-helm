---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ingress-smoke
  labels:
    app.kubernetes.io/name: ingress-smoke
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: ingress-smoke
  template:
    metadata:
      labels:
        app.kubernetes.io/name: ingress-smoke
    spec:
      containers:
        - name: nginx
          image: docker.io/nginx:latest
          ports:
            - containerPort: 80

---
apiVersion: v1
kind: Service
metadata:
  name: ingress-smoke
  labels:
    app.kubernetes.io/name: ingress-smoke
spec:
  selector:
    app.kubernetes.io/name: ingress-smoke
  ports:
    - name: http
      port: 80
      targetPort: 80
